apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastapi-mvc-example.fullname" . }}-test-connection"
  labels:
    {{- include "fastapi-mvc-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastapi-mvc-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
